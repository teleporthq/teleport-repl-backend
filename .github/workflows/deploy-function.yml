name: Deploy GCF

on:
  push:
    branches:
      - master
    paths:
      - "functions/getQuotesByDate/*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Install and build
        uses: actions/setup-node@v1
        with:
          node-version: 12.x
      - run: |
          yarn
          yarn build
        env:
          CI: "true"
      - name: Deploy to cloud function
        uses: actions/gcloud/auth@master
        env:
          GCLOUD_AUTH: ${{ secrets.GCLOUD_AUTH }}
      - run: |
          gcloud functions deploy repl-api --trigger-http --runtime nodejs10 --entry-point app --set-env-vars NODE_ENV=production --project croapp-dev
